<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMathGraph">
        <activity android:name=".CircleFunction"></activity>
        <activity
            android:name=".SinusCosinus"
            android:screenOrientation="landscape" />
        <activity android:name=".SeveralGraphs" />
        <activity android:name=".QuadraticFunction" />
        <activity android:name=".LinearFunction" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>